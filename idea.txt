# print(row)
        # Write following info for each row
        # writer.writerow({
        #     "name": row["name"],
        #     "date":  "12/02/2020",
        #     "items": "big lol"
        # })
    # Should rewrite the file by renaming the temp file the same name as original file.
    # shutil.move(temp_file.name, filename)
# ------ My Idea ------
# Store array of names without repeating repeated entries
    # Go through each row containing the specified name as selected in the array
        # Append specified info to selected string variable for column we want to append to



with open('new.csv', "r") as csvfile:
    reader = csv.DictReader(csvfile)
    for row in reader:
        print(row)

def append_data(file_path, name, product, amount, date, unit_cost):
    fieldnames = ['name','product','amount','date','unit_cost']
    # Append data to existing file
    with open(file_path, "a") as csvfile:
        writer = csv.DictWriter(csvfile, fieldnames=fieldnames)
        writer.writerow({"name": name, 
                        "product": product, 
                        "amount": amount + " Litres", 
                        "date": date, 
                        "unit_cost": "£"+unit_cost
                        })


# with open(filename, "r") as csvfile, temp_file:
#     # Will be reading from the simple csv file
#     reader = csv.DictReader(csvfile)
#     # fieldnames for new CSV file
#     fieldnames = ['name','date', 'items']
#     # Will be writing to a temp_file csv using the fieldnames above
#     writer = csv.DictWriter(temp_file, fieldnames=fieldnames)
#     # Writes column headers
#     writer.writeheader()
#     # For every row in csvfile
#     for row in reader:
#         # write each new row
#         writer.writerow({
#             'name': row['name'],
#             'date': row['date'],
#             'items': '[{\"name\":\"' + row['date'] +  " - " + row['product'] + "\",\"quantity\":" + row['amount'] + ", \"unit_cost\"" + row["unit_cost"] + "}]"
#         })
#     shutil.move(temp_file.name, "USETHIS.csv")